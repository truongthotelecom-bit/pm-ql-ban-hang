# Deployment Rules
- Do NOT automatically commit, push to GitHub, or deploy to Vercel/production unless explicitly requested by the user. 
- By default, run and test code locally (e.g., using `npm run dev`).
